Sample application for tracking Bowling score.

Setup and run:
This app requires at least ruby 1.9
Run `bundle install` in app directory and then run `rackup`
Navigate to `localhost:9292` in a web browser of your choice.
To execute tests, run `bundle exec rspec`
To check test coverage, open file `index.html` in `coverage` directory.
--
